## 工作规则

- 始终在仓库根目录工作，不依赖固定绝对路径。
- 不要回退或覆盖无关用户改动；改动范围保持收敛。
- 搜索优先用 `rg`，验证优先跑定向测试或语法检查。
- 如需安装依赖、访问网络、启动服务或执行受限命令，先请求权限。
- 不确定业务规则时先确认；明确的代码/文档同步问题可以直接处理。
- apply_patch 在Windows 环境中不可用，请改用 Python 或 PowerShell 编辑文件。

