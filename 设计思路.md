1.前端校验逻辑和后端校验逻辑不统一，比如说前端可以设置面积为0并保存，但是点击仿真的时候报错了。请检查是否还有类似问题并修改；
2.前端报错信息不够完整，1中错误在打开浏览器f12调式工具时只返回了Internal Server Error，没有具体的错误信息。请提供处理方案；
3.功能分区做成表格，增加批量建立、复制、修改、删除分区的功能
 完成以上任务后，调用'ask-user' tool 等待我进一步指示（必须要有用户自定义输入的选项）。
