1.请参考design/DESIGN.md，优化网页的UI设计。
 完成以上任务后，调用'ask-user' tool 等待我进一步指示（必须要有用户自定义输入的选项）。


你的修改效果我不是很满意，我已经取消了前面所有的修改。现在请你专注于解决下面的问题：
1. 建筑配置页面（BuildingView）存在多个功能分区时，页面会非常卡顿，比如建立了500个分区时，各种的操作时间会非常久，请采用优化手段；
2. 同样的当功能分区比较多时，调用energyplus计算耗时较长，会导致接口超时报错。请试着多种角度去优化这个问题：是否可以将计算任务放到后台，然后可以在一个位置显示计算进度；energyplus是否可以采用一定的并行计算去加快速度；是否可以优化idf提升计算速度等。你可以参考一下我的建议，最好还能提出自己的想法。有任何不确定的可以通过'ask-user' tool”向我确认（必须要有用户自定义输入的选项）。
 完成以上任务后，调用'ask-user' tool 等待我进一步指示（必须要有用户自定义输入的选项）。
