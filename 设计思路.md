1. 不同的设备（冷机、水泵、冷却塔、风冷模块等）需要分开建表，我现在发现还在一张表当中。原来的哪些数据都不要，请直接根据data/equipment中json文件重新创建表格。现有的方案出现错误没事，限制是测试阶段，我会手动删除这些方案。另外把气象数据见表一起放到script中的文件中吧。
2. 在选型时每次点开搜索框都要加载（从后端获取数据）显得卡卡的，能不能优化。
3. 设备搜索框存在一个bug。比如输入范围的最小值>最大值之后，搜索框就会直接卡死，后面即使再次点击型号输入框也不会弹出搜索框
4. 搜索框里面样式优化，范围输入 不要有min、max和调整按钮，剩下的你自己优化。

1. 展开系统后会因为出现了滚动条导致页面元素跳动，解决一下这个问题（设备选型和控制策略都存在）。该问题还是没有解决
2. 新建系统时没有默认创建一个"冷机-冷冻水泵-冷却水泵组合"(风冷模块-水泵组合)


请根据problem.md所列问题清单，依次排查并修复UI/UX问题。每个问题需要先告诉我发生的原因和你准备的解决方案后，调用'ask-user' tool 等待我确认（必须要有用户自定义输入的选项）再行修改

针对控制策略页面
1. 日程表名称和通用时间表被压缩，无法正常显示（其实空间足够横向显示即可）
2. 全天、夜间、清空三个按钮分散对齐显示

1. 全选这里应该有个选择模式切换，正常模式下就只能单击点选某一个分区（不显示多选框）。批量选择模式下可以多选分区（点击卡片和多选框均可）。卡片中点击复制和删除按钮时，事件不应冒泡到然后触发选择卡片事件。
2. 不仅是这个页面的按钮，整个项目所有页面的按钮都应该统一管理样式

性能还是需要优化。 关闭选择模式或者在选择模式下选择第一个分区时，页面会卡一会请优化



1. 方案卡片需要展示的内容包括建筑名称(面积)、含制冷（峰值负荷、装机容量、累计制冷量、耗电量）、制热（峰值负荷、装机容量、累计制冷量、耗电量）
2. 参考一下1.png优化一下建筑卡片和方案卡片的样式


1. 移动端制冷制热不要换行展示，适当调小字号，也不用加粗
2. 你给方案卡片用的那个图标应该是建筑用的，方案换一个图标
3. 安全裕量不用显示出来。建筑面积也不用了，变成建筑名（子系统数）
4. 制冷制热的图标优化一下啊，别只是写一个字
1. 桌面端卡片样式和移动端保持一致啊
2. 建筑和方案名称也不用加粗

1. 我为移动端的工程界面下面导航设计了图标，放在了mobile-nav-icons文件夹下。有两组，分别是给选中及未选中状态使用的。现在移动端的选中状态（比如建筑搭建被选中了），请将图标替换成选中状态的图标然后给一个动效（比如缩放，你可以试试看还有没有更好的效果），去掉现在的框选加背景的样式（太丑了）

1. 选中状态和未选择状态的图标尺寸保持一样大（按现在选中状态的图标大小）
2. 移动端点击时会有一个矩形方框显示的效果，请取消之
3. 移动端、桌面端方案卡片和建筑卡片的方案名称和建筑名称统一为16px

1. 桌面端建筑卡片、方案卡片、移动端的方案卡片和 移动端的建筑卡片的影音、边框等样式不一致，请改为一致，最好再style.css中统一管理（你可以检查一下其他页面涉及到这种卡片的样式）

我发现是手机的问题，已经取消了刚刚的修改。下面进入另一个问题

1. 按钮改成圆形，每个按钮上方是图标下方是文本
2. 卡片区域下方不用预留空白区域给按钮

1. 移动端新增建筑方案的弹出没有在当前显示位置，而是跑到页面上发了，遮罩层也只在页面最上面，而不是当前显示的位置。

完成以上任务后，调用'ask-user' tool 等待我进一步指示（必须要有用户自定义输入的选项）

