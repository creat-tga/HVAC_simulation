1. 不同的设备（冷机、水泵、冷却塔、风冷模块等）需要分开建表，我现在发现还在一张表当中。原来的哪些数据都不要，请直接根据data/equipment中json文件重新创建表格。现有的方案出现错误没事，限制是测试阶段，我会手动删除这些方案。另外把气象数据见表一起放到script中的文件中吧。
2. 在选型时每次点开搜索框都要加载（从后端获取数据）显得卡卡的，能不能优化。
3. 设备搜索框存在一个bug。比如输入范围的最小值>最大值之后，搜索框就会直接卡死，后面即使再次点击型号输入框也不会弹出搜索框
4. 搜索框里面样式优化，范围输入 不要有min、max和调整按钮，剩下的你自己优化。

1. 展开系统后会因为出现了滚动条导致页面元素跳动，解决一下这个问题（设备选型和控制策略都存在）。该问题还是没有解决
2. 新建系统时没有默认创建一个"冷机-冷冻水泵-冷却水泵组合"(风冷模块-水泵组合)


请根据problem.md所列问题清单，依次排查并修复UI/UX问题。每个问题需要先告诉我发生的原因和你准备的解决方案后，调用'ask-user' tool 等待我确认（必须要有用户自定义输入的选项）再行修改


1. 每个系统只能显示4个组合是怎么回事，剩下的看不到 
完成以上任务后，调用'ask-user' tool 等待我进一步指示（必须要有用户自定义输入的选项）

