# BuildingView 日程冲突检测代码分析

## 代码功能

### `getConflicts(schedules: DaySchedule[])`

**作用**：检测一组日程（同一参数下的所有日程组）中哪些组对之间存在时间冲突。

**逻辑**：
- 使用双重循环（`i` < `j`），对每一对日程组调用 `schedulesConflict(a, b)` 判断是否冲突
- 返回所有冲突对的索引数组 `[number, number][]`，例如 `[[0, 2], [1, 3]]` 表示第 0 组和第 2 组冲突、第 1 组和第 3 组冲突

**冲突判定条件**（三个维度全部重叠才算冲突）：
1. **日期范围重叠** — 两个日程组的 `start_month/start_day ~ end_month/end_day` 存在交集
2. **星期重叠** — 两组选中的星期存在交集（如都包含周一）
3. **小时重叠** — 两组选中的小时存在交集（如都包含 9 点）

### `hasAnyConflict` computed

**作用**：全局检查当前建筑所有分区、所有内扰参数（人员密度、照明、设备、新风）中是否存在任何日程冲突。

**逻辑**：
- 遍历 `editZones` 中所有分区
- 对每个分区的 4 个 `ParamConfig`（由 `PARAM_METAS` 定义），如果该参数处于 `scheduled` 模式，则调用 `getConflicts` 检查
- 一旦发现任何冲突，立即返回 `true`（短路）

**用途**：
- 控制「保存」按钮的 `disabled` 状态
- 点击保存时，如果 `hasAnyConflict === true`，弹出错误提示并阻止保存

---

## 执行效率分析

### 时间复杂度

设：
- $Z$ = 分区数量
- $P$ = 参数数量（固定 4：人员/照明/设备/新风）
- $N$ = 单个参数的日程组数量（上限 20）

#### `getConflicts`

- 双重循环：$O(N^2)$ 次比较
- 每次 `schedulesConflict` 调用：
  - `dateRangesOverlap`：$O(1)$（纯数值比较）
  - `days` 交集：$O(|days_a| \times |days_b|)$，最大 $7 \times 7 = 49$，即 $O(1)$
  - `hours` 交集：$O(|hours_a| \times |hours_b|)$，最大 $24 \times 24 = 576$，即 $O(1)$

单次 `getConflicts` 总复杂度：$O(N^2)$

由于 $N \leq 20$，最多 $\binom{20}{2} = 190$ 次比较，每次 $O(1)$，实际约 190 次简单操作。

#### `hasAnyConflict`

- 遍历所有分区和参数：$O(Z \times P)$
- 每个参数调用一次 `getConflicts`：$O(N^2)$

总复杂度：$O(Z \times P \times N^2) = O(Z \times 4 \times 400) = O(1600Z)$

### 实际影响评估

| 场景 | 分区数 $Z$ | 日程组数 $N$ | 总比较次数 | 耗时估算 |
|------|-----------|-------------|-----------|---------|
| 小型项目 | 5 | 3 | 5 × 4 × 3 = 60 | < 0.01ms |
| 中型项目 | 50 | 5 | 50 × 4 × 10 = 2000 | < 0.1ms |
| 大型项目 | 200 | 10 | 200 × 4 × 45 = 36000 | < 1ms |
| 极端场景 | 1000 | 20 | 1000 × 4 × 190 = 760000 | ~5ms |

### 结论

**当前实现的执行效率完全足够**，原因如下：

1. **常量级上限**：日程组数量限制为 20，`days` 最多 7 个，`hours` 最多 24 个，每次比较都是常量级操作
2. **短路机制**：`hasAnyConflict` 一旦发现冲突立即返回 `true`，不会继续遍历
3. **Vue 响应式缓存**：`computed` 属性只在其依赖变化时重新计算，不会每帧执行
4. **实际规模**：即使 1000 个分区、每个参数 20 个日程组的极端场景，总计算量也只需约 5ms，远低于 16ms（60fps）的帧预算

### 如果未来需要优化

对于超大规模场景（>1000 分区），可以考虑：
- **延迟计算**：使用 `watchDebounce` 延迟冲突检测，避免每次修改立即触发
- **局部检测**：只检查当前活动分区（`activeZone`）的冲突，而非全局遍历
- **增量更新**：修改某个日程组时只重新检查该组与其他组的冲突，而非全量重算

但按照当前 20 组上限的设计，这些优化是不必要的。
