# 多系统融合仿真平台立项计划

## 一、项目背景

### 1.1 行业现状

随着"双碳"目标的深入推进，建筑节能与暖通空调（HVAC）系统优化已成为建筑行业的核心议题。据统计，建筑能耗约占我国社会总能耗的 30%，其中暖通空调系统能耗占建筑总能耗的 40%~60%。在建筑全生命周期中，设计阶段对建筑能效的影响最为深远，而精准的仿真分析是实现高效设计的关键手段。

### 1.2 现有痛点

当前 HVAC 系统仿真面临以下主要挑战：

1. **工具碎片化**：建筑负荷计算（EnergyPlus）、设备选型（厂商软件）、系统运行模拟（TRNSYS 等）分属不同工具，数据流转困难、协同效率低下。
2. **专业壁垒高**：传统仿真工具（如 EnergyPlus、OpenStudio）学习成本高，要求工程师具备深厚的模型构建能力，限制了技术普及。
3. **多系统耦合不足**：冷水机组、水泵、冷却塔、热泵、自然冷却等子系统之间的耦合关系难以在单一平台中完整建模，导致仿真结果与实际运行偏差较大。
4. **决策链路割裂**：从负荷预测到系统方案比选到运行策略优化，缺乏一体化的决策支撑流程，导致设计人员需要在多个平台间手动串联分析结果。
5. **精细化运行时间粒度缺失**：大多数工具仅支持月度或设计日负荷分析，难以覆盖全年 8760 小时的逐时运行特性。

### 1.3 市场需求

- 设计院需要快速对比不同系统方案的全年能耗与经济性
- 业主希望在项目前期获得直观可靠的系统能效评估与投资回报分析
- 运维团队需要基于实际负荷特征优化运行策略
- "双碳"政策要求精确计算建筑碳排放，需贯穿从设计到运维的全生命周期

---

## 二、项目方案

### 2.1 平台定位

构建一个 **"建筑负荷—暖通系统—运行策略"** 全链路的多系统融合仿真平台，以 Web 应用形式提供服务。平台覆盖从建筑围护结构建模到系统方案对比、全年逐时能耗模拟、经济性分析和碳排放评估的完整工作流。

### 2.2 技术架构

```
┌──────────────────────────────────────────────────────┐
│                     前端展示层                        │
│   Vue 3 + TypeScript + Element Plus + ECharts        │
│   建筑配置 │ 系统选型 │ 仿真运行 │ 报表分析           │
├──────────────────────────────────────────────────────┤
│                     后端服务层                        │
│   FastAPI + SQLAlchemy + Pydantic v2                 │
│   项目管理 │ 用户认证 │ 数据持久化 │ 任务调度          │
├──────────────────────────────────────────────────────┤
│                    仿真引擎层                         │
│  ┌────────────┐  ┌─────────────┐  ┌───────────────┐ │
│  │ 负荷计算模块 │  │ 系统仿真模块 │  │ 运行策略模块  │ │
│  │ EnergyPlus  │  │ 组件化建模   │  │ 控制逻辑优化  │ │
│  │ 8760h逐时   │  │ 多系统耦合   │  │ 变频/变流量   │ │
│  └────────────┘  └─────────────┘  └───────────────┘ │
├──────────────────────────────────────────────────────┤
│                     数据层                            │
│   SQLite/PostgreSQL │ 气象数据库 │ 设备参数库         │
└──────────────────────────────────────────────────────┘
```

### 2.3 核心模块设计

#### 2.3.1 建筑负荷仿真模块

- **功能分区建模**：支持将建筑划分为多个功能分区（办公、会议、走廊、大堂等），每个分区独立设置围护结构参数和内部扰量
- **分时段参数配置**：人员密度、照明功率、设备功率等内部扰量支持按「日期范围 × 星期 × 小时」三维度精细化定义，确保全年 8760 小时的内扰特征完整覆盖
- **EnergyPlus 引擎集成**：自动生成 IDF 文件，调用 EnergyPlus 执行逐时负荷计算
- **负荷预览**：在系统配置前提供冷/热负荷预览图表，辅助前期决策

#### 2.3.2 多系统仿真模块

支持以下 HVAC 系统类型的独立建模与对比分析：

| 系统类型 | 核心设备 | 特点 |
|---------|---------|------|
| 风冷冷水系统 | 风冷冷水机组 | 适用于缺水地区，安装简便 |
| 水冷冷水系统 | 水冷冷水机组 + 冷却塔 + 水泵 | 能效高，适用于大型建筑 |
| 地源热泵系统 | 地埋管换热器 + 热泵机组 | 可再生能源利用，冷热兼供 |
| 自然冷却系统 | 板式换热器 + 冷却塔 | 过渡季节利用自然冷源，大幅降低能耗 |
| 复合系统 | 以上系统的组合 | 提供灵活的冷源切换策略 |

每种系统以组件化方式建模，模型参数包括额定工况性能曲线、部分负荷特性曲线（PLR）、辅助设备能耗模型等。

#### 2.3.3 经济性与碳排放分析模块

- **电价模型**：支持固定电价、分时电价（峰/平/谷）、阶梯电价及其组合模式
- **投资估算**：基于系统配置自动估算初投资
- **运行费用**：基于逐时能耗与电价策略计算全年运行电费
- **碳排放计算**：根据地区电力碳排放因子计算年碳排放量
- **方案对比**：多系统方案的能耗、费用、碳排放并列对比图表

#### 2.3.4 报表与可视化模块

- **逐时负荷曲线**：全年 8760 小时冷/热负荷瀑布图
- **系统能耗分项**：冷机、水泵、冷却塔等各子系统能耗占比
- **运行成本分析**：月度/年度能耗费用统计与趋势图
- **方案比选报告**：自动生成多方案对比分析报告，支持导出 PDF

### 2.4 技术选型

| 层级 | 技术栈 | 说明 |
|-----|--------|------|
| 前端 | Vue 3 + TypeScript + Vite | 组合式 API，响应式编程 |
| UI 组件 | Element Plus + ECharts | 企业级组件库 + 专业图表 |
| 后端 | Python 3.12 + FastAPI | 异步高性能 API 框架 |
| 数据库 | SQLite / PostgreSQL | 轻量开发 / 生产级部署 |
| ORM | SQLAlchemy 2.0 (Async) | 异步数据库访问 |
| 仿真引擎 | EnergyPlus + Python 科学计算 | 工业级仿真 + 灵活扩展 |
| 包管理 | pnpm (前端) / uv (后端) | 高效依赖管理 |
| 国际化 | vue-i18n | 中英双语支持 |

### 2.5 开发阶段规划

| 阶段 | 内容 |
|-----|------|
| **第一阶段** | 基础平台搭建：用户认证、项目管理、建筑配置（分区+分时段内扰）、EnergyPlus 负荷计算集成 |
| **第二阶段** | 系统仿真引擎：风冷/水冷冷水系统、地源热泵系统建模，全年逐时仿真 |
| **第三阶段** | 高级功能：自然冷却系统、复合系统、运行策略优化、方案对比分析 |
| **第四阶段** | 完善与扩展：报表导出、设备参数数据库、多用户协作、云端部署 |

---

## 三、项目目标

### 3.1 总体目标

建设一个面向 HVAC 设计与优化的 **多系统融合仿真平台**，实现从建筑围护结构定义到系统方案评估的全链路数字化仿真，为建筑节能设计提供一站式决策支持工具。

### 3.2 具体目标

1. **全链路贯通**：打通"建筑负荷 → 系统仿真 → 经济分析 → 碳排放评估"的完整数据流，消除传统工作流中的数据孤岛
2. **精细化建模**：
   - 支持功能分区级别的建筑建模，每个分区独立定义围护结构和内部扰量
   - 内部扰量参数支持「日期范围 × 星期 × 小时」三维时间粒度，精确描述全年 8760 小时运行特征
3. **多系统覆盖**：支持不少于 4 种主流 HVAC 系统类型的独立仿真与横向对比
4. **降低使用门槛**：
   - 提供建筑类型模板和功能分区预设，降低参数输入复杂度
   - Web 化交互界面，无需安装专业软件
   - 智能预填充和参数校验，减少输入错误
5. **辅助决策**：提供能耗、费用、碳排放三维度的多方案对比分析能力，直观呈现各方案优劣
6. **灵活适配**：支持多种电价策略（固定/分时/阶梯及组合），适配不同地区的电力价格机制

### 3.3 预期成果

- 可交付的 Web 仿真平台（前后端完整系统）
- 建筑负荷仿真引擎（EnergyPlus 集成，支持逐时计算）
- 多系统能耗仿真引擎（组件化建模，支持部分负荷特性）
- 技术文档与用户手册

---

## 四、创新点

### 4.1 全链路多系统融合仿真架构

区别于传统单一工具（EnergyPlus 仅做负荷计算、TRNSYS 侧重系统仿真），本平台创新性地将建筑负荷仿真与多种 HVAC 系统仿真在统一架构下融合。通过标准化的数据接口，负荷计算结果自动流转至系统仿真模块，无需手动导入导出，消除了工具间的数据断层。

### 4.2 三维时间粒度的内扰参数模型

传统仿真工具通常采用"时间表"（Schedule）机制定义内部扰量，配置过程繁琐且不直观。本平台提出 **「日期范围 × 星期 × 小时」三维时间粒度模型**：

- **日期范围维度**：以月/日精度定义适用日期段（如 5月1日~9月30日）
- **星期维度**：通过复选框选择适用的星期若干天
- **小时维度**：通过 0~23 小时网格直观选择适用时段

三个维度正交组合，既能精确覆盖全年 8760 小时的每一个时刻，又提供了高效直观的交互方式。同时内置冲突检测机制，自动识别日程组之间的时间重叠，确保参数定义的唯一性和完整性。

### 4.3 组件化系统建模与即时对比

将 HVAC 系统拆解为标准化组件（冷水机组、水泵、冷却塔、换热器、地埋管等），通过组件组合构建不同系统方案。这种组件化架构带来以下优势：

- **灵活组合**：同类设备可快速替换（如风冷机组替换为水冷机组），无需重建整个模型
- **即时对比**：多套系统方案可并行运行仿真，结果即时并列呈现
- **可扩展性**：新增系统类型只需开发对应组件，无需修改框架

### 4.4 功能分区感知的精细化建模

突破传统"整栋建筑统一参数"的建模方式，引入功能分区概念：

- 一栋建筑可包含上千个功能分区（办公、会议室、走廊、大堂、餐厅等）
- 每个分区独立配置围护结构参数和内部扰量
- 提供分区模板与预设，支持快速批量创建
- 通过可搜索选择器高效管理大量分区

这种分区级建模更贴近真实建筑的使用特征，使仿真结果更为精准。

### 4.5 低门槛 Web 化仿真体验

将专业级 HVAC 仿真能力封装为易用的 Web 应用：

- **零安装**：无需在本地部署 EnergyPlus 等专业软件，浏览器即可使用
- **智能预设**：根据建筑类型自动推荐围护结构参数和内扰参数
- **分步引导**：建筑配置 → 负荷预览 → 系统选型 → 仿真分析的渐进式工作流
- **实时反馈**：参数修改后即时展示影响，支持快速迭代设计方案
- **国际化**：中英双语支持，适应不同用户群体

### 4.6 多维经济性评估模型

集成灵活多样的电价模型（固定/分时/阶梯及其组合），结合逐时能耗仿真结果进行精准的运行费用核算。相比传统估算方式，可反映不同电价策略对运行成本的真实影响，为方案比选提供更有说服力的经济性依据。

---

## 五、总结

本项目定位于构建一套多系统融合仿真平台，以 Web 化、组件化、精细化为核心设计理念，解决当前 HVAC 仿真领域工具碎片化、专业壁垒高、多系统耦合不足等关键痛点。通过全链路数据贯通、三维时间粒度内扰模型、组件化系统建模等创新设计，为建筑节能设计提供高效、精准、易用的一站式仿真分析工具，助力"双碳"目标的实现。